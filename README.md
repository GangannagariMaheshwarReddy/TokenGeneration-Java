# 🔐 JWT Authentication using Spring Boot

This project demonstrates **JWT (JSON Web Token) based Authentication and Authorization** using Spring Boot and Spring Security.

It implements stateless authentication where the server generates a JWT token after successful login, and the client uses that token for accessing protected APIs.

---

## 🚀 Features

- User Login Authentication
- JWT Token Generation
- JWT Token Validation
- Stateless Authentication
- Spring Security Integration
- Role-Based Authorization
- Protected REST APIs

---

## 🏗️ Tech Stack

- Java 17+
- Spring Boot
- Spring Security
- JWT (io.jsonwebtoken)
- Maven
- REST APIs

---

## 📌 What is JWT?

JWT (JSON Web Token) is a compact, URL-safe token format used for secure data transmission between client and server.

It consists of:

1. Header
2. Payload
3. Signature
